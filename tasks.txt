task-1: Run a background task to simulate sensor polling.
cd iot_logger
cd scripts
nano sensor_script.py
    import time
    import random

    while True:
        temp = round(random.uniform(10.0, 30.0), 2)
        print(f"Temperature: {temp}°C")
        time.sleep(10)

python3 sensor_script.py &

task-2: List processes and filter for the background task.
ps aux | grep sensor.py

task-3: Check network states (established connections). 
netstat -tuln | grep ESTABLISHED

task-4: Try foreground and background switching.
cd iot_logger
cd scripts
python3 sensor_script.py #ctrl+z to stop it 
bg #to run it in the background
fg #to run it in the foreground

task-5: Kill a process if needed.
kill 33523  #kill PID process
